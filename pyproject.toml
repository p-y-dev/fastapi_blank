[tool.poetry]
name = "fastapi-simple"
version = "0.1.0"
description = ""
authors = ["Pavel Yakovlev <py@theinvaders.pro>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
